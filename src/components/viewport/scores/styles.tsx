import { StyleSheet } from 'aphrodite';
import "typeface-roboto";

export default StyleSheet.create({
    scores: {
        alignItems: 'center',
        display: 'flex',
        flexFlow: 'column',
        fontFamily: 'roboto',
    }
});